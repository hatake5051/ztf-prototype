version: '3'
services:
  cap:
    build:
      context: ../../
      dockerfile: ./example/cap/Dockerfile
    hostname: cap1.example.com
    environment:
      VIRTUAL_HOST: cap1.example.com
    ports:
    - "80"
    networks:
    - reverse-proxy-net

networks:
  reverse-proxy-net:
    external: true